<h1>タスク一覧</h1>

<table>
    <tr>
        <th>ID</th>
        <th>タスクの内容</th>
    </tr>
    <% @tasks.each do |task| %>
    <tr>
        <td><%= link_to task.id, task_path(task) %></td>
        <td><%= task.content %></td>
    </tr>
    <% end %>
</table>

<%= link_to "新規作成", new_task_path %>